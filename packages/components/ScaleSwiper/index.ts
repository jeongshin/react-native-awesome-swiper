import FlatList, { ScaleSwiperProps } from './FlatList';
import Provider from '../../contexts/Provider';
import DotIndicator from '../DotIndicator';

export default {
  FlatList,
  Provider,
  DotIndicator,
};

export type { ScaleSwiperProps };
